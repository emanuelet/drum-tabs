{
    "name": "drum-tabs-cloud",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "cd .. && deno task build-frontend",
        "dev": "wrangler --config ../wrangler.local.jsonc dev",
        "deploy": "npm run build && wrangler --config ../wrangler.local.jsonc deploy",
        "test": "vitest run .",
        "typecheck": "tsc --project tsconfig.json --noEmit"
    },
    "dependencies": {
        "@better-auth/drizzle-adapter": "^1.6.25",
        "better-auth": "catalog:",
        "drizzle-orm": "^0.45.2",
        "hono": "^4.11.4",
        "zod": "catalog:"
    },
    "devDependencies": {
        "@types/node": "catalog:",
        "typescript": "catalog:",
        "vitest": "^3.2.4",
        "wrangler": "^4.36.0"
    }
}
