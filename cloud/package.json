{
    "name": "drum-tabs-cloud",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "cd .. && deno task build-frontend",
        "dev": "wrangler --config ../wrangler.local.jsonc dev",
        "deploy": "deno run build && wrangler --config ../wrangler.local.jsonc deploy",
        "test": "vitest run .",
        "typecheck": "tsc --project tsconfig.json --noEmit"
    },
    "dependencies": {
        "@better-auth/drizzle-adapter": "^1.7.2",
        "better-auth": "catalog:",
        "drizzle-orm": "^0.45.2",
        "hono": "^4.13.7",
        "zod": "catalog:"
    },
    "devDependencies": {
        "@types/node": "catalog:",
        "typescript": "catalog:",
        "vitest": "catalog:",
        "wrangler": "^4.129.0"
    }
}
