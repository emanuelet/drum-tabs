{
    "name": "drum-tabs-cloud",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "deno task build-frontend",
        "dev:cloud": "wrangler dev",
        "deploy:cloud": "npm run build && wrangler deploy",
        "test:cloud": "vitest run cloud",
        "typecheck:cloud": "tsc --project cloud/tsconfig.json --noEmit"
    },
    "dependencies": {
        "@coderline/alphatab": "^1.8.4",
        "@hono/zod-validator": "^0.9.0",
        "@better-auth/drizzle-adapter": "^1.6.25",
        "better-auth": "catalog:",
        "drizzle-orm": "^0.45.2",
        "hono": "^4.11.4",
        "zod": "catalog:"
    },
    "devDependencies": {
        "@types/node": "catalog:",
        "typescript": "catalog:",
        "vitest": "^3.2.4",
        "wrangler": "^4.36.0"
    }
}
